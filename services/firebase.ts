// Arquivo obsoleto após remoção do Firebase.
// Mantido vazio para evitar erros de build caso existam referências residuais, 
// mas não deve ser importado.
export const auth = null;
export const db = null;
export const getAppId = () => 'local-app';
export const getInitialAuthToken = () => undefined;